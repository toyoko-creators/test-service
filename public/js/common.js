$('.slick01').slick(
{
    centerMode: true,
    centerPadding: '20%',
    // dots: true,
    variableWidth: true,
    slidesToShow: 1,
    slidesToScroll: 1,
    infinite: false,      // スライドをループしない
    focusOnSelect: true,  // 中央以外をクリックすると、中央まで移動(=フォーカス)する。
    
  });

  $('.slick02').slick(
    {
        centerMode: true,
        centerPadding: '20%',
        // dots: true,
        variableWidth: true,
        slidesToShow: 1,
        slidesToScroll: 1,
        infinite: false,      // スライドをループしない
        focusOnSelect: true,  // 中央以外をクリックすると、中央まで移動(=フォーカス)する。
        
    });
